# Factory Constitution

This document defines the repository’s structure, conventions, and governance.

**Section 3.1 – Folder structure**

- `apps/api` – API application
- `apps/web` – Web application
- `packages/shared` – Shared code
- `packages/ui` – UI package
- `infra` – Infrastructure

Replace this placeholder with the full constitution when available.
